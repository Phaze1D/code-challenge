import * as React from 'react';
import * as s from './Preview.styles';

const UI: React.FC<{

}> = ({

}) => {

  return (
    <s.Container>
      Markdown
    </s.Container>
  );
};
export default UI;
